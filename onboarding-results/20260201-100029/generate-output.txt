Looking at the extracted slots for MacPorts, I'll generate a comprehensive lore package that captures the tribal knowledge around this complex package manager. MacPorts has specific requirements around Xcode dependencies, OS migrations, and conflict avoidance that need careful handling.

```json
{
  "package_name": "macports",
  "files": {
    "lifecycle": {
      "filename": "lifecycle.yaml",
      "content": "name: macports\nversion: \"2.11.6\"\ndescription: An easy-to-use system for compiling, installing, and managing open source software on macOS\nhomepage: https://www.macports.org\nrepository: https://github.com/macports/macports-base\nlicense: BSD-3-Clause\n\nauthors:\n  - name: The MacPorts Project\n    url: https://www.macports.org\n\nplatforms:\n  - Darwin\n\nfeatures:\n  trace-mode:\n    description: Enable trace mode to avoid /usr/local interference during port builds\n    default: false\n  binary-archives:\n    description: Use prebuilt binary archives when available (faster than source builds)\n    default: true\n  universal-variant:\n    description: Build universal binaries supporting multiple architectures\n    default: false\n\nsettings:\n  macos-version:\n    type: string\n    default: \"auto\"\n    description: Target macOS version for installer selection (auto-detects current OS)\n  installation-method:\n    type: string\n    default: \"pkg\"\n    description: Installation method (pkg, source, git)\n    allowed_values: [\"pkg\", \"source\", \"git\"]\n  prefix:\n    type: string\n    default: \"/opt/local\"\n    description: Installation prefix (only used for source installations)\n\ndependencies:\n  - name: xcode-command-line-tools\n    required: true\n    description: Apple's Command Line Developer Tools (xcode-select --install)\n  - name: xcode\n    required: false\n    description: Apple's Xcode Developer Tools (required for some ports)\n\nconflicts:\n  - name: homebrew\n    reason: Both use different prefixes but can interfere during builds\n    severity: warning\n  - name: fink\n    reason: Package managers can conflict in dependency resolution\n    severity: warning\n\nverification:\n  command: \"port version\"\n  pattern: \"Version: \\\\d+\\\\.\\\\d+\\\\.\\\\d+\"\n\ntags:\n  - package-manager\n  - macos\n  - ports\n  - source-builds"
    },
    "readme": {
      "filename": "README.md",
      "content": "# MacPorts\n\nMacPorts is an easy-to-use system for compiling, installing, and managing open source software on macOS. It provides a way to install Unix software that has been ported to work on macOS, using a package management system similar to those found on Linux distributions.\n\n## Operations\n\n### Deploy\nFresh installation of MacPorts on a clean system:\n- **prepare**: Checks for Xcode Command Line Tools and resolves conflicts with /usr/local software\n- **install**: Downloads and installs MacPorts using the appropriate method (pkg installer, source, or git)\n- **provision**: Configures shell environment and runs initial selfupdate\n- **verify**: Confirms MacPorts is working and can access the ports tree\n\n### Upgrade\nUpgrades MacPorts to a newer version:\n- **prepare**: Backs up port list and checks for OS compatibility\n- **upgrade**: Installs new MacPorts version over existing installation\n- **migrate**: Runs migration procedure to rebuild ports for new version\n- **verify**: Confirms upgrade was successful and ports are functional\n\n### Decommission\nCompletely removes MacPorts and all installed ports:\n- **unprovision**: Removes shell environment configuration\n- **uninstall**: Removes all installed ports and MacPorts itself\n- **cleanup**: Removes directories, users, and configuration files\n\n## Features\n\n| Feature | Description | Default |\n|---------|-------------|---------|\n| trace-mode | Enable trace mode to hide /usr/local during builds | false |\n| binary-archives | Use prebuilt binaries when available | true |\n| universal-variant | Build universal binaries for multiple architectures | false |\n\n## Settings\n\n| Setting | Type | Default | Description |\n|---------|------|---------|-------------|\n| macos-version | string | auto | Target macOS version for installer selection |\n| installation-method | string | pkg | Installation method (pkg, source, git) |\n| prefix | string | /opt/local | Installation prefix (source installs only) |\n\n## Tribal Knowledge\n\n### Migration Required for Major OS Changes\n**Location**: `Darwin/Upgrade/migrate.star:15-45`\n\nMacPorts installations don't survive major macOS upgrades or CPU architecture changes. The migration procedure (`sudo port migrate`) is essential but can fail if the base system is too outdated. In such cases, a complete reinstall is required.\n\n### Avoid /usr/local Conflicts  \n**Location**: `Darwin/Deploy/prepare.star:20-35`\n\nSoftware installed in `/usr/local` can cause MacPorts builds to fail because some configure scripts look for dependencies there first. Use trace mode (`port -t install`) to hide these conflicts during problematic builds.\n\n### Version-Specific Package Installers\n**Location**: `Darwin/Deploy/install.star:25-50`\n\nMacPorts provides different .pkg installers for each macOS version. The URLs follow a predictable pattern, but using the wrong version can cause compatibility issues. Always match the installer to your exact macOS version.\n\n### Xcode License Acceptance\n**Location**: `Darwin/Deploy/provision.star:30-40`\n\nWith Xcode 4 and later, the license must be accepted before MacPorts can use development tools. This is often forgotten and causes cryptic build failures.\n\n### Regular Updates Essential\n**Location**: `Darwin/Deploy/provision.star:50-60`\n\nRunning `sudo port selfupdate` regularly keeps both the MacPorts base system and the ports tree current. Outdated installations can fail to build newer software or miss security updates.\n\n### Shell Environment Setup\n**Location**: `Darwin/Deploy/provision.star:65-85`\n\nThe .pkg installer automatically configures shell environments, but source installations require manual PATH setup. The order matters: MacPorts paths must come before system paths to avoid conflicts.\n\n## Sources\n\n- [MacPorts Installation Guide](https://www.macports.org/install.php) - Primary installation documentation\n- [MacPorts Migration Guide](https://trac.macports.org/wiki/Migration) - OS upgrade procedures\n- [MacPorts FAQ](https://trac.macports.org/wiki/FAQ) - Common issues and solutions\n- [MacPorts Guide](https://guide.macports.org) - Comprehensive user guide"
    }
  },
  "platforms": {
    "Darwin": {
      "Deploy": {
        "prepare": {
          "filename": "Darwin/Deploy/prepare.star",
          "content": "# macports/Darwin/Deploy/prepare.star - Prepare phase for MacPorts installation\n#\n# TRIBAL KNOWLEDGE:\n# Software in /usr/local can interfere with MacPorts builds because some\n# configure scripts look there first. We check for common problematic packages\n# and warn the user. We don't automatically remove them since they might be\n# needed, but we document the trace-mode workaround.\n\ndef prepare():\n    \"\"\"Check preconditions for MacPorts installation.\"\"\"\n    _check_xcode_tools()\n    _check_conflicting_software()\n    _check_macos_version()\n    return {\"prepare\": True}\n\ndef _check_xcode_tools():\n    \"\"\"Verify Xcode Command Line Tools are installed.\"\"\"\n    if not fs.exists(\"/Library/Developer/CommandLineTools\"):\n        # Try to trigger installation\n        shell.exec(\n            \"xcode-select --install\",\n            allowed_commands=[\"xcode-select\"]\n        )\n        fail(\"Xcode Command Line Tools installation initiated. Please complete and retry.\")\n    \n    # Check if tools are properly configured\n    result = shell.exec(\n        \"xcode-select -p\",\n        allowed_commands=[\"xcode-select\"]\n    )\n    if not result.success:\n        fail(\"Xcode Command Line Tools not properly configured. Run: xcode-select --install\")\n\ndef _check_conflicting_software():\n    \"\"\"Check for software that commonly conflicts with MacPorts builds.\"\"\"\n    conflicting_paths = [\n        \"/usr/local/bin\",\n        \"/usr/local/lib\", \n        \"/Library/Frameworks\"\n    ]\n    \n    warnings = []\n    for path in conflicting_paths:\n        if fs.exists(path):\n            # Check if it contains significant software\n            if path == \"/usr/local/bin\" and fs.exists(\"/usr/local/bin/brew\"):\n                warnings.append(\"Homebrew detected in /usr/local - may interfere with MacPorts builds\")\n            elif path == \"/usr/local/lib\":\n                libs = shell.exec(f\"ls {path} | head -5\", allowed_commands=[\"ls\", \"head\"])\n                if libs.success and libs.stdout.strip():\n                    warnings.append(f\"Libraries found in {path} - may cause build conflicts\")\n            elif path == \"/Library/Frameworks\":\n                frameworks = shell.exec(f\"ls {path} | grep -v System | head -3\", allowed_commands=[\"ls\", \"grep\", \"head\"])\n                if frameworks.success and frameworks.stdout.strip():\n                    warnings.append(f\"Non-system frameworks in {path} - may interfere with builds\")\n    \n    if warnings:\n        print(\"WARNING: Potential conflicts detected:\")\n        for warning in warnings:\n            print(f\"  - {warning}\")\n        print(\"  Consider using trace mode: 'sudo port -t install <portname>'\")\n\ndef _check_macos_version():\n    \"\"\"Ensure macOS version is supported.\"\"\"\n    # MacPorts supports macOS 10.4+ but practical minimum is much higher\n    if not platform.version_gte(\"10.15\"):\n        print(\"WARNING: macOS versions older than 10.15 have limited port support\")\n    \n    # Check for very new versions that might not have installers yet\n    version_parts = platform.version.split(\".\")\n    major_version = int(version_parts[0])\n    if major_version > 15:  # Adjust as new versions are released\n        print(f\"WARNING: macOS {platform.version} is very new - pkg installer may not be available\")\n        print(\"Consider using source installation method\")\n\ndef rollback():\n    \"\"\"No rollback needed for prepare phase.\"\"\"\n    pass"
        },
        "install": {
          "filename": "Darwin/Deploy/install.star",
          "content": "# macports/Darwin/Deploy/install.star - Install phase for MacPorts\n#\n# TRIBAL KNOWLEDGE:\n# MacPorts provides version-specific .pkg installers for each macOS release.\n# Using the wrong installer can cause compatibility issues. The URLs follow\n# a predictable pattern: MacPorts-{version}-{os_version}.pkg\n# We auto-detect the OS version but allow override via settings.\n\ndef install():\n    \"\"\"Install MacPorts using the specified method.\"\"\"\n    method = env.get(\"installation_method\", \"pkg\")\n    \n    if method == \"pkg\":\n        _install_via_pkg()\n    elif method == \"source\":\n        _install_via_source()\n    elif method == \"git\":\n        _install_via_git()\n    else:\n        fail(f\"Unknown installation method: {method}\")\n    \n    return {\"install\": True}\n\ndef _install_via_pkg():\n    \"\"\"Install using macOS package installer.\"\"\"\n    version = \"2.11.6\"\n    \n    # Map macOS versions to installer names\n    os_version_map = {\n        \"15\": \"15-Sequoia\",\n        \"14\": \"14-Sonoma\", \n        \"13\": \"13-Ventura\",\n        \"12\": \"12-Monterey\",\n        \"11\": \"11-BigSur\",\n        \"10.15\": \"10.15-Catalina\",\n        \"10.14\": \"10.14-Mojave\",\n        \"10.13\": \"10.13-HighSierra\"\n    }\n    \n    # Determine OS version for installer selection\n    macos_version = env.get(\"macos_version\", \"auto\")\n    if macos_version == \"auto\":\n        # Auto-detect based on current OS\n        os_version = platform.version.split(\".\")[0]\n        if platform.version.startswith(\"10.\"):\n            os_version = \".\".join(platform.version.split(\".\")[:2])\n    else:\n        os_version = macos_version\n    \n    if os_version not in os_version_map:\n        print(f\"WARNING: No specific installer for macOS {os_version}, trying latest\")\n        installer_suffix = \"15-Sequoia\"  # Fallback to latest\n    else:\n        installer_suffix = os_version_map[os_version]\n    \n    pkg_filename = f\"MacPorts-{version}-{installer_suffix}.pkg\"\n    pkg_url = f\"https://github.com/macports/macports-base/releases/download/v{version}/{pkg_filename}\"\n    temp_pkg = f\"/tmp/{pkg_filename}\"\n    \n    # Download installer\n    print(f\"Downloading MacPorts installer for macOS {os_version}\")\n    http.download(pkg_url, temp_pkg)\n    \n    # Install package\n    print(\"Installing MacPorts...\")\n    shell.exec(\n        f\"sudo installer -pkg {temp_pkg} -target /\",\n        allowed_commands=[\"installer\"]\n    )\n    \n    # Cleanup\n    fs.remove(temp_pkg)\n\ndef _install_via_source():\n    \"\"\"Install from source tarball.\"\"\"\n    version = \"2.11.6\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    \n    tarball_url = f\"https://distfiles.macports.org/MacPorts/MacPorts-{version}.tar.bz2\"\n    temp_dir = f\"/tmp/MacPorts-{version}\"\n    tarball = f\"{temp_dir}.tar.bz2\"\n    \n    # Download and extract\n    print(f\"Downloading MacPorts {version} source...\")\n    http.download(tarball_url, tarball)\n    \n    print(\"Extracting source...\")\n    shell.exec(\n        f\"cd /tmp && tar xjf MacPorts-{version}.tar.bz2\",\n        allowed_commands=[\"tar\"]\n    )\n    \n    # Configure, build, and install\n    print(f\"Configuring with prefix {prefix}...\")\n    shell.exec(\n        f\"cd {temp_dir} && ./configure --prefix={prefix}\",\n        allowed_commands=[\"configure\"]\n    )\n    \n    print(\"Building MacPorts...\")\n    shell.exec(\n        f\"cd {temp_dir} && make\",\n        allowed_commands=[\"make\"]\n    )\n    \n    print(\"Installing MacPorts...\")\n    shell.exec(\n        f\"cd {temp_dir} && sudo make install\",\n        allowed_commands=[\"make\"]\n    )\n    \n    # Cleanup\n    fs.remove(temp_dir)\n    fs.remove(tarball)\n\ndef _install_via_git():\n    \"\"\"Install from Git repository (development version).\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    temp_dir = \"/tmp/macports-base\"\n    \n    # Clone repository\n    print(\"Cloning MacPorts repository...\")\n    if fs.exists(temp_dir):\n        fs.remove(temp_dir)\n    \n    shell.exec(\n        f\"cd /tmp && git clone https://github.com/macports/macports-base.git\",\n        allowed_commands=[\"git\"]\n    )\n    \n    # Configure, build, and install\n    print(f\"Configuring with prefix {prefix}...\")\n    shell.exec(\n        f\"cd {temp_dir} && ./configure --prefix={prefix}\",\n        allowed_commands=[\"configure\"]\n    )\n    \n    print(\"Building MacPorts...\")\n    shell.exec(\n        f\"cd {temp_dir} && make\",\n        allowed_commands=[\"make\"]\n    )\n    \n    print(\"Installing MacPorts...\")\n    shell.exec(\n        f\"cd {temp_dir} && sudo make install\",\n        allowed_commands=[\"make\"]\n    )\n    \n    # Cleanup\n    fs.remove(temp_dir)\n\ndef rollback():\n    \"\"\"Remove MacPorts installation.\"\"\"\n    # Try to remove MacPorts using its own uninstall procedure\n    if fs.exists(\"/opt/local/bin/port\"):\n        # Remove all installed ports first\n        shell.exec(\n            \"sudo /opt/local/bin/port -f uninstall installed\",\n            allowed_commands=[\"/opt/local/bin/port\"]\n        )\n    \n    # Remove MacPorts directories\n    directories_to_remove = [\n        \"/opt/local\",\n        \"/Applications/DarwinPorts\", \n        \"/Applications/MacPorts\",\n        \"/Library/LaunchDaemons/org.macports.*\",\n        \"/Library/Receipts/DarwinPorts*.pkg\",\n        \"/Library/Receipts/MacPorts*.pkg\"\n    ]\n    \n    for directory in directories_to_remove:\n        if \"*\" in directory:\n            shell.exec(\n                f\"sudo rm -rf {directory}\",\n                allowed_commands=[\"rm\"]\n            )\n        elif fs.exists(directory):\n            fs.remove(directory)\n    \n    # Remove users and groups\n    shell.exec(\n        \"sudo dscl . -delete /Users/macports\", \n        allowed_commands=[\"dscl\"]\n    )\n    shell.exec(\n        \"sudo dscl . -delete /Groups/macports\",\n        allowed_commands=[\"dscl\"] \n    )"
        },
        "provision": {
          "filename": "Darwin/Deploy/provision.star",
          "content": "# macports/Darwin/Deploy/provision.star - Provision phase for MacPorts\n#\n# TRIBAL KNOWLEDGE:\n# The .pkg installer automatically configures shell environments by adding\n# MacPorts paths to /opt/local/etc/macports/mp-buildbot.conf, but source installs\n# require manual PATH setup. The order is critical: MacPorts paths must come\n# before system paths to prevent conflicts.\n\ndef provision():\n    \"\"\"Configure MacPorts for use.\"\"\"\n    _setup_shell_environment()\n    _accept_xcode_license()\n    _run_initial_selfupdate()\n    _configure_features()\n    return {\"provision\": True}\n\ndef _setup_shell_environment():\n    \"\"\"Configure shell environment for MacPorts.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    \n    # Check if PATH already contains MacPorts\n    current_path = env.get(\"PATH\", \"\")\n    macports_bin = f\"{prefix}/bin\"\n    macports_sbin = f\"{prefix}/sbin\"\n    \n    if macports_bin in current_path and macports_sbin in current_path:\n        print(\"MacPorts already in PATH\")\n        return\n    \n    # For pkg installations, shell config should be automatic\n    if fs.exists(\"/Library/Receipts/MacPorts*.pkg\") or fs.exists(\"/var/db/receipts/org.macports.MacPorts.plist\"):\n        print(\"Package installation detected - shell environment should be configured automatically\")\n        print(\"If 'port' command not found, restart your shell or run:\")\n        print(f\"  export PATH={macports_bin}:{macports_sbin}:$PATH\")\n        return\n    \n    # For source/git installations, we need to configure manually\n    shell_configs = [\n        (\"~/.bash_profile\", \"bash_profile\"),\n        (\"~/.bashrc\", \"bashrc\"), \n        (\"~/.zshrc\", \"zshrc\"),\n        (\"~/.profile\", \"profile\")\n    ]\n    \n    path_export = f\"export PATH={macports_bin}:{macports_sbin}:$PATH\"\n    manpath_export = f\"export MANPATH={prefix}/share/man:$MANPATH\"\n    \n    for config_path, shell_type in shell_configs:\n        expanded_path = env.expand(config_path)\n        if fs.exists(expanded_path):\n            content = fs.read(expanded_path)\n            if prefix not in content:\n                print(f\"Adding MacPorts to {config_path}\")\n                updated_content = content + \"\\n# MacPorts\\n\" + path_export + \"\\n\" + manpath_export + \"\\n\"\n                fs.write(expanded_path, updated_content)\n\ndef _accept_xcode_license():\n    \"\"\"Accept Xcode license if needed.\"\"\"\n    # Check if Xcode is installed\n    if not fs.exists(\"/Applications/Xcode.app\"):\n        print(\"Xcode not installed - skipping license acceptance\")\n        return\n    \n    # Check license status\n    result = shell.exec(\n        \"xcodebuild -license check\",\n        allowed_commands=[\"xcodebuild\"]\n    )\n    \n    if not result.success:\n        print(\"Xcode license needs acceptance\")\n        print(\"Please run: sudo xcodebuild -license accept\")\n        print(\"Or accept license through Xcode GUI\")\n        # Don't fail here, just warn - Command Line Tools might be sufficient\n\ndef _run_initial_selfupdate():\n    \"\"\"Run initial selfupdate to sync ports tree.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if not fs.exists(port_cmd):\n        fail(f\"MacPorts binary not found at {port_cmd}\")\n    \n    print(\"Running initial selfupdate...\")\n    result = shell.exec(\n        f\"sudo {port_cmd} -v selfupdate\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        print(\"WARNING: Initial selfupdate failed\")\n        print(\"This might indicate network issues or incomplete installation\")\n        print(f\"Try running manually: sudo {port_cmd} selfupdate\")\n\ndef _configure_features():\n    \"\"\"Configure MacPorts features based on settings.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    config_file = f\"{prefix}/etc/macports/macports.conf\"\n    \n    if not fs.exists(config_file):\n        print(f\"WARNING: MacPorts config file not found at {config_file}\")\n        return\n    \n    config_updates = []\n    \n    # Configure binary archives\n    if env.get(\"binary_archives\", \"true\") == \"true\":\n        config_updates.append(\"# Enable binary archives for faster installs\\nbuildfromsource never\")\n    else:\n        config_updates.append(\"# Always build from source\\nbuildfromsource always\")\n    \n    # Configure trace mode default\n    if env.get(\"trace_mode\", \"false\") == \"true\":\n        config_updates.append(\"# Enable trace mode by default to avoid /usr/local conflicts\\ntrace yes\")\n    \n    # Configure universal variant\n    if env.get(\"universal_variant\", \"false\") == \"true\":\n        config_updates.append(\"# Enable universal variant by default\\nuniversal_variant yes\")\n    \n    if config_updates:\n        current_config = fs.read(config_file)\n        updated_config = current_config + \"\\n\\n# Lore package configuration\\n\" + \"\\n\\n\".join(config_updates) + \"\\n\"\n        shell.exec(\n            f\"echo '{updated_config}' | sudo tee {config_file} > /dev/null\",\n            allowed_commands=[\"echo\", \"tee\"]\n        )\n\ndef rollback():\n    \"\"\"Remove MacPorts configuration.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    \n    # Remove shell environment configuration\n    shell_configs = [\n        \"~/.bash_profile\",\n        \"~/.bashrc\",\n        \"~/.zshrc\", \n        \"~/.profile\"\n    ]\n    \n    for config_path in shell_configs:\n        expanded_path = env.expand(config_path)\n        if fs.exists(expanded_path):\n            content = fs.read(expanded_path)\n            if \"MacPorts\" in content:\n                # Remove MacPorts-related lines\n                lines = content.split(\"\\n\")\n                filtered_lines = []\n                skip_next = False\n                \n                for line in lines:\n                    if \"# MacPorts\" in line:\n                        skip_next = True\n                        continue\n                    if skip_next and (prefix in line or \"MANPATH\" in line):\n                        skip_next = False\n                        continue\n                    filtered_lines.append(line)\n                \n                fs.write(expanded_path, \"\\n\".join(filtered_lines))"
        },
        "verify": {
          "filename": "Darwin/Deploy/verify.star",
          "content": "# macports/Darwin/Deploy/verify.star - Verify phase for MacPorts installation\n\ndef verify():\n    \"\"\"Verify MacPorts installation is working correctly.\"\"\"\n    _check_port_command()\n    _check_ports_tree()\n    _check_basic_functionality()\n    return {\"verify\": True}\n\ndef _check_port_command():\n    \"\"\"Verify port command is available and working.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if not fs.exists(port_cmd):\n        fail(f\"MacPorts binary not found at {port_cmd}\")\n    \n    # Test version command\n    result = shell.exec(\n        f\"{port_cmd} version\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts version command failed\")\n    \n    # Verify version pattern\n    if \"Version:\" not in result.stdout:\n        fail(f\"Unexpected version output: {result.stdout}\")\n    \n    print(f\"MacPorts version check passed: {result.stdout.strip()}\")\n\ndef _check_ports_tree():\n    \"\"\"Verify ports tree is accessible.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    # Test searching for a common port\n    result = shell.exec(\n        f\"{port_cmd} search --name --glob 'curl'\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts search command failed - ports tree may not be synced\")\n    \n    if \"curl\" not in result.stdout:\n        print(\"WARNING: Search results seem limited - ports tree may be outdated\")\n        print(\"Consider running: sudo port selfupdate\")\n    else:\n        print(\"Ports tree search working correctly\")\n\ndef _check_basic_functionality():\n    \"\"\"Test basic MacPorts functionality.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    # Test info command on a simple port\n    result = shell.exec(\n        f\"{port_cmd} info curl\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts info command failed\")\n    \n    if \"curl\" not in result.stdout.lower():\n        fail(\"MacPorts info command returned unexpected results\")\n    \n    print(\"Basic MacPorts functionality verified\")\n    \n    # Check for common issues\n    _check_common_issues()\n\ndef _check_common_issues():\n    \"\"\"Check for common MacPorts installation issues.\"\"\"\n    warnings = []\n    \n    # Check for Xcode Command Line Tools\n    if not fs.exists(\"/Library/Developer/CommandLineTools\"):\n        warnings.append(\"Xcode Command Line Tools not found - may cause build failures\")\n    \n    # Check for conflicting software in /usr/local\n    if fs.exists(\"/usr/local/bin\") and fs.exists(\"/usr/local/bin/brew\"):\n        warnings.append(\"Homebrew detected in /usr/local - may cause build conflicts\")\n    \n    # Check PATH order\n    current_path = env.get(\"PATH\", \"\")\n    path_components = current_path.split(\":\")\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    macports_bin_index = -1\n    usr_local_bin_index = -1\n    \n    for i, component in enumerate(path_components):\n        if component == f\"{prefix}/bin\":\n            macports_bin_index = i\n        elif component == \"/usr/local/bin\":\n            usr_local_bin_index = i\n    \n    if macports_bin_index > -1 and usr_local_bin_index > -1:\n        if usr_local_bin_index < macports_bin_index:\n            warnings.append(\"/usr/local/bin appears before MacPorts in PATH - may cause conflicts\")\n    \n    if warnings:\n        print(\"\\nWARNINGS detected:\")\n        for warning in warnings:\n            print(f\"  - {warning}\")\n        print(\"\\nMacPorts should still work, but you may encounter issues with some ports.\")\n    else:\n        print(\"No common issues detected\")\n\ndef rollback():\n    \"\"\"No rollback needed for verify phase.\"\"\"\n    pass"
        }
      },
      "Upgrade": {
        "prepare": {
          "filename": "Darwin/Upgrade/prepare.star",
          "content": "# macports/Darwin/Upgrade/prepare.star - Prepare phase for MacPorts upgrade\n\ndef prepare():\n    \"\"\"Prepare for MacPorts upgrade.\"\"\"\n    _backup_port_list()\n    _check_os_compatibility()\n    _check_disk_space()\n    return {\"prepare\": True}\n\ndef _backup_port_list():\n    \"\"\"Backup list of currently installed ports.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if fs.exists(port_cmd):\n        backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n        result = shell.exec(\n            f\"{port_cmd} -qv installed > {backup_file}\",\n            allowed_commands=[port_cmd]\n        )\n        \n        if result.success:\n            print(f\"Port list backed up to {backup_file}\")\n        else:\n            print(\"WARNING: Could not backup port list\")\n    else:\n        print(\"MacPorts not found - skipping backup\")\n\ndef _check_os_compatibility():\n    \"\"\"Check if current OS is compatible with upgrade.\"\"\"\n    # Check for major OS version changes that require migration\n    version_parts = platform.version.split(\".\")\n    major_version = int(version_parts[0])\n    \n    # Store current OS version for comparison in migrate phase\n    env.set(\"pre_upgrade_os_version\", platform.version)\n    \n    if major_version >= 15:  # Recent macOS versions\n        print(f\"macOS {platform.version} detected - upgrade should proceed normally\")\n    else:\n        print(f\"WARNING: macOS {platform.version} is older - verify MacPorts support\")\n\ndef _check_disk_space():\n    \"\"\"Check available disk space for upgrade.\"\"\"\n    result = shell.exec(\n        \"df -h /opt/local | tail -1 | awk '{print $4}'\",\n        allowed_commands=[\"df\", \"tail\", \"awk\"]\n    )\n    \n    if result.success:\n        available = result.stdout.strip()\n        print(f\"Available disk space: {available}\")\n        # Basic check - should have at least 1GB free\n        if \"M\" in available or (\"G\" in available and float(available.replace(\"G\", \"\")) < 1.0):\n            print(\"WARNING: Low disk space may cause upgrade issues\")\n    else:\n        print(\"Could not check disk space\")\n\ndef rollback():\n    \"\"\"Remove backup files.\"\"\"\n    backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n    if fs.exists(backup_file):\n        fs.remove(backup_file)"
        },
        "upgrade": {
          "filename": "Darwin/Upgrade/upgrade.star",
          "content": "# macports/Darwin/Upgrade/upgrade.star - Upgrade phase for MacPorts\n#\n# TRIBAL KNOWLEDGE:\n# MacPorts upgrades are more complex than simple package updates. Major OS\n# changes often require the migration procedure instead of a simple upgrade.\n# The safest approach is to install the new version over the old one using\n# the appropriate installer for the current OS.\n\ndef upgrade():\n    \"\"\"Upgrade MacPorts to the latest version.\"\"\"\n    _check_upgrade_method()\n    _perform_upgrade()\n    return {\"upgrade\": True}\n\ndef _check_upgrade_method():\n    \"\"\"Determine the appropriate upgrade method.\"\"\"\n    current_os = platform.version\n    pre_upgrade_os = env.get(\"pre_upgrade_os_version\", current_os)\n    \n    if current_os != pre_upgrade_os:\n        print(f\"OS version changed from {pre_upgrade_os} to {current_os}\")\n        print(\"This requires migration procedure, not simple upgrade\")\n        env.set(\"requires_migration\", \"true\")\n    else:\n        print(\"Same OS version - standard upgrade procedure\")\n        env.set(\"requires_migration\", \"false\")\n\ndef _perform_upgrade():\n    \"\"\"Perform the actual MacPorts upgrade.\"\"\"\n    requires_migration = env.get(\"requires_migration\", \"false\")\n    \n    if requires_migration == \"true\":\n        _upgrade_with_migration()\n    else:\n        _upgrade_in_place()\n\ndef _upgrade_in_place():\n    \"\"\"Upgrade MacPorts in place (same OS version).\"\"\"\n    # Try selfupdate first\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if fs.exists(port_cmd):\n        print(\"Attempting selfupdate...\")\n        result = shell.exec(\n            f\"sudo {port_cmd} selfupdate\",\n            allowed_commands=[port_cmd]\n        )\n        \n        if result.success:\n            print(\"MacPorts selfupdate completed successfully\")\n            return\n        else:\n            print(\"Selfupdate failed, trying installer method\")\n    \n    # Fall back to installer method\n    _install_new_version()\n\ndef _upgrade_with_migration():\n    \"\"\"Upgrade MacPorts when OS version has changed.\"\"\"\n    print(\"OS version change detected - installing new MacPorts version\")\n    _install_new_version()\n\ndef _install_new_version():\n    \"\"\"Install new MacPorts version using appropriate installer.\"\"\"\n    version = \"2.11.6\"\n    \n    # Use same logic as install phase to get correct installer\n    os_version_map = {\n        \"15\": \"15-Sequoia\",\n        \"14\": \"14-Sonoma\",\n        \"13\": \"13-Ventura\", \n        \"12\": \"12-Monterey\",\n        \"11\": \"11-BigSur\",\n        \"10.15\": \"10.15-Catalina\",\n        \"10.14\": \"10.14-Mojave\",\n        \"10.13\": \"10.13-HighSierra\"\n    }\n    \n    os_version = platform.version.split(\".\")[0]\n    if platform.version.startswith(\"10.\"):\n        os_version = \".\".join(platform.version.split(\".\")[:2])\n    \n    if os_version not in os_version_map:\n        print(f\"WARNING: No specific installer for macOS {os_version}, trying latest\")\n        installer_suffix = \"15-Sequoia\"\n    else:\n        installer_suffix = os_version_map[os_version]\n    \n    pkg_filename = f\"MacPorts-{version}-{installer_suffix}.pkg\"\n    pkg_url = f\"https://github.com/macports/macports-base/releases/download/v{version}/{pkg_filename}\"\n    temp_pkg = f\"/tmp/{pkg_filename}\"\n    \n    print(f\"Downloading MacPorts {version} for macOS {os_version}...\")\n    http.download(pkg_url, temp_pkg)\n    \n    print(\"Installing new MacPorts version...\")\n    shell.exec(\n        f\"sudo installer -pkg {temp_pkg} -target /\",\n        allowed_commands=[\"installer\"]\n    )\n    \n    fs.remove(temp_pkg)\n    print(\"MacPorts upgrade installation completed\")\n\ndef rollback():\n    \"\"\"Rollback upgrade attempt.\"\"\"\n    print(\"Rollback from upgrade is complex - consider restoring from backup\")\n    print(\"Or run migration procedure to rebuild ports\")\n    \n    backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n    if fs.exists(backup_file):\n        print(f\"Port list backup available at: {backup_file}\")\n    \n    # Clean up any temporary files\n    temp_files = [\"/tmp/MacPorts-*.pkg\"]\n    for pattern in temp_files:\n        shell.exec(\n            f\"rm -f {pattern}\",\n            allowed_commands=[\"rm\"]\n        )"
        },
        "migrate": {
          "filename": "Darwin/Upgrade/migrate.star",
          "content": "# macports/Darwin/Upgrade/migrate.star - Migration phase for MacPorts upgrade\n#\n# TRIBAL KNOWLEDGE:\n# The migration procedure is essential after OS upgrades or architecture changes.\n# It uninstalls all ports, updates the ports tree, and reinstalls everything\n# with the new compiler/libraries. This can take hours for systems with many ports.\n# If migration fails, manual reinstallation may be required.\n\ndef migrate():\n    \"\"\"Run MacPorts migration procedure.\"\"\"\n    requires_migration = env.get(\"requires_migration\", \"false\")\n    \n    if requires_migration == \"true\":\n        _run_migration_procedure()\n    else:\n        _update_ports_tree()\n    \n    return {\"migrate\": True}\n\ndef _run_migration_procedure():\n    \"\"\"Run full MacPorts migration procedure.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if not fs.exists(port_cmd):\n        fail(f\"MacPorts not found at {port_cmd} - upgrade may have failed\")\n    \n    print(\"Starting MacPorts migration procedure...\")\n    print(\"WARNING: This may take a very long time if you have many ports installed\")\n    \n    # First, try the built-in migration\n    print(\"Attempting automatic migration...\")\n    result = shell.exec(\n        f\"sudo {port_cmd} migrate\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if result.success:\n        print(\"Automatic migration completed successfully\")\n        _verify_migration()\n    else:\n        print(\"Automatic migration failed, trying manual procedure\")\n        _manual_migration()\n\ndef _manual_migration():\n    \"\"\"Perform manual migration procedure.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n    \n    # Check if we have a backup of installed ports\n    if not fs.exists(backup_file):\n        print(\"Creating current port list backup...\")\n        shell.exec(\n            f\"{port_cmd} -qv installed > {backup_file}\",\n            allowed_commands=[port_cmd]\n        )\n    \n    # Uninstall all ports\n    print(\"Uninstalling all ports...\")\n    result = shell.exec(\n        f\"sudo {port_cmd} -f uninstall installed\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        print(\"WARNING: Some ports failed to uninstall cleanly\")\n    \n    # Clean up\n    print(\"Cleaning up...\")\n    shell.exec(\n        f\"sudo {port_cmd} clean --all all\",\n        allowed_commands=[port_cmd]\n    )\n    \n    # Update ports tree\n    print(\"Updating ports tree...\")\n    shell.exec(\n        f\"sudo {port_cmd} selfupdate\",\n        allowed_commands=[port_cmd]\n    )\n    \n    # Reinstall ports from backup\n    if fs.exists(backup_file):\n        print(\"Reinstalling ports from backup...\")\n        content = fs.read(backup_file)\n        lines = content.strip().split(\"\\n\")\n        \n        # Extract port names (first column)\n        port_names = []\n        for line in lines:\n            if line.strip() and not line.startswith(\"The following\"):\n                parts = line.split()\n                if parts:\n                    port_names.append(parts[0])\n        \n        if port_names:\n            print(f\"Reinstalling {len(port_names)} ports...\")\n            ports_to_install = \" \".join(port_names)\n            shell.exec(\n                f\"sudo {port_cmd} install {ports_to_install}\",\n                allowed_commands=[port_cmd]\n            )\n        else:\n            print(\"No ports found in backup file\")\n    else:\n        print(\"No backup file available - manual port reinstallation required\")\n\ndef _update_ports_tree():\n    \"\"\"Update ports tree for standard upgrades.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    print(\"Updating ports tree...\")\n    result = shell.exec(\n        f\"sudo {port_cmd} selfupdate\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if result.success:\n        print(\"Ports tree updated successfully\")\n    else:\n        print(\"WARNING: Ports tree update failed\")\n\ndef _verify_migration():\n    \"\"\"Verify migration was successful.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    # Check that port command works\n    result = shell.exec(\n        f\"{port_cmd} version\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"Migration verification failed - port command not working\")\n    \n    # Check installed ports\n    result = shell.exec(\n        f\"{port_cmd} installed\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if result.success:\n        installed_count = len([line for line in result.stdout.split(\"\\n\") \n                             if line.strip() and \"@\" in line])\n        print(f\"Migration verified - {installed_count} ports currently installed\")\n    else:\n        print(\"WARNING: Could not verify installed ports\")\n\ndef rollback():\n    \"\"\"Attempt to rollback migration.\"\"\"\n    print(\"Migration rollback is complex and may not be fully reversible\")\n    \n    backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n    if fs.exists(backup_file):\n        print(f\"Original port list available at: {backup_file}\")\n        print(\"You can attempt to reinstall from this list manually\")\n    \n    # The migration process is largely irreversible, but we can suggest recovery\n    print(\"To recover from failed migration:\")\n    print(\"1. Check port list backup file\")\n    print(\"2. Manually reinstall critical ports\")\n    print(\"3. Consider complete MacPorts reinstall if needed\")"
        },
        "verify": {
          "filename": "Darwin/Upgrade/verify.star",
          "content": "# macports/Darwin/Upgrade/verify.star - Verify phase for MacPorts upgrade\n\ndef verify():\n    \"\"\"Verify MacPorts upgrade was successful.\"\"\"\n    _check_version_upgrade()\n    _check_ports_functionality()\n    _check_migration_success()\n    return {\"verify\": True}\n\ndef _check_version_upgrade():\n    \"\"\"Verify MacPorts version was upgraded.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    result = shell.exec(\n        f\"{port_cmd} version\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts version command failed after upgrade\")\n    \n    version_output = result.stdout.strip()\n    expected_version = \"2.11.6\"\n    \n    if expected_version in version_output:\n        print(f\"MacPorts upgrade verified: {version_output}\")\n    else:\n        print(f\"WARNING: Version may not have upgraded - got: {version_output}\")\n        print(f\"Expected version {expected_version}\")\n\ndef _check_ports_functionality():\n    \"\"\"Verify basic MacPorts functionality after upgrade.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    # Test search functionality\n    result = shell.exec(\n        f\"{port_cmd} search --name --glob curl\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts search failed after upgrade\")\n    \n    if \"curl\" not in result.stdout:\n        fail(\"MacPorts search returned no results - ports tree may be corrupted\")\n    \n    # Test info command\n    result = shell.exec(\n        f\"{port_cmd} info curl\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"MacPorts info command failed after upgrade\")\n    \n    print(\"Basic MacPorts functionality verified after upgrade\")\n\ndef _check_migration_success():\n    \"\"\"Check if migration was successful (if it was required).\"\"\"\n    requires_migration = env.get(\"requires_migration\", \"false\")\n    \n    if requires_migration != \"true\":\n        print(\"Migration was not required - skipping migration verification\")\n        return\n    \n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    backup_file = \"/tmp/macports-installed-before-upgrade.txt\"\n    \n    # Check current installed ports\n    result = shell.exec(\n        f\"{port_cmd} installed\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if not result.success:\n        fail(\"Cannot check installed ports after migration\")\n    \n    current_ports = [line for line in result.stdout.split(\"\\n\") \n                     if line.strip() and \"@\" in line]\n    current_count = len(current_ports)\n    \n    # Compare with backup if available\n    if fs.exists(backup_file):\n        backup_content = fs.read(backup_file)\n        backup_lines = [line for line in backup_content.split(\"\\n\")\n                       if line.strip() and not line.startswith(\"The following\")]\n        backup_count = len(backup_lines)\n        \n        print(f\"Migration results: {current_count} ports installed (was {backup_count})\")\n        \n        if current_count == 0 and backup_count > 0:\n            print(\"WARNING: No ports installed after migration - may have failed\")\n            print(f\"Original port list backed up at: {backup_file}\")\n        elif current_count < backup_count * 0.8:  # Less than 80% restored\n            print(f\"WARNING: Significantly fewer ports installed after migration\")\n            print(f\"Consider checking migration logs or reinstalling missing ports\")\n        else:\n            print(\"Migration appears successful\")\n    else:\n        print(f\"Migration completed - {current_count} ports currently installed\")\n        print(\"No backup available for comparison\")\n    \n    # Test installing a simple port to verify build environment\n    print(\"Testing port installation capability...\")\n    result = shell.exec(\n        f\"sudo {port_cmd} install recutils +universal 2>&1 | head -10\",\n        allowed_commands=[port_cmd, \"head\"]\n    )\n    \n    if \"Error\" in result.stdout or \"Failed\" in result.stdout:\n        print(\"WARNING: Test port installation showed errors\")\n        print(\"Build environment may need attention\")\n    else:\n        print(\"Port installation capability verified\")\n        # Clean up test install\n        shell.exec(\n            f\"sudo {port_cmd} uninstall recutils\",\n            allowed_commands=[port_cmd]\n        )\n\ndef rollback():\n    \"\"\"No rollback needed for verify phase.\"\"\"\n    pass"
        }
      },
      "Decommission": {
        "unprovision": {
          "filename": "Darwin/Decommission/unprovision.star",
          "content": "# macports/Darwin/Decommission/unprovision.star - Unprovision phase for MacPorts removal\n\ndef unprovision():\n    \"\"\"Remove MacPorts configuration and stop any services.\"\"\"\n    _remove_shell_configuration()\n    _stop_background_processes()\n    return {\"unprovision\": True}\n\ndef _remove_shell_configuration():\n    \"\"\"Remove MacPorts PATH and environment configuration.\"\"\"\n    shell_configs = [\n        \"~/.bash_profile\",\n        \"~/.bashrc\", \n        \"~/.zshrc\",\n        \"~/.profile\"\n    ]\n    \n    prefix = env.get(\"prefix\", \"/opt/local\")\n    \n    for config_path in shell_configs:\n        expanded_path = env.expand(config_path)\n        if fs.exists(expanded_path):\n            content = fs.read(expanded_path)\n            \n            if prefix in content or \"MacPorts\" in content:\n                print(f\"Removing MacPorts configuration from {config_path}\")\n                \n                # Remove MacPorts-related lines\n                lines = content.split(\"\\n\")\n                filtered_lines = []\n                skip_section = False\n                \n                for line in lines:\n                    if \"# MacPorts\" in line:\n                        skip_section = True\n                        continue\n                    \n                    if skip_section:\n                        if (prefix in line or \n                            \"MANPATH\" in line and \"macports\" in line.lower() or\n                            line.strip() == \"\"):\n                            if line.strip() != \"\":\n                                continue\n                            else:\n                                skip_section = False\n                        else:\n                            skip_section = False\n                    \n                    if not skip_section:\n                        filtered_lines.append(line)\n                \n                fs.write(expanded_path, \"\\n\".join(filtered_lines))\n    \n    print(\"MacPorts shell configuration removed\")\n    print(\"Note: Restart your shell or source config files for changes to take effect\")\n\ndef _stop_background_processes():\n    \"\"\"Stop any MacPorts-related background processes.\"\"\"\n    # MacPorts doesn't typically run background services, but check for any\n    result = shell.exec(\n        \"ps aux | grep -i macports | grep -v grep\",\n        allowed_commands=[\"ps\", \"grep\"]\n    )\n    \n    if result.success and result.stdout.strip():\n        print(\"Found MacPorts-related processes:\")\n        print(result.stdout)\n        print(\"You may need to manually stop these processes\")\n    else:\n        print(\"No MacPorts background processes found\")\n    \n    # Check for any LaunchDaemons\n    launchd_paths = [\n        \"/Library/LaunchDaemons/org.macports.*\",\n        \"/Library/LaunchAgents/org.macports.*\"\n    ]\n    \n    for path_pattern in launchd_paths:\n        result = shell.exec(\n            f\"ls {path_pattern} 2>/dev/null\",\n            allowed_commands=[\"ls\"]\n        )\n        \n        if result.success and result.stdout.strip():\n            print(f\"Found LaunchDaemon files: {result.stdout}\")\n            print(\"These will be removed in the cleanup phase\")\n\ndef rollback():\n    \"\"\"Restore shell configuration if needed.\"\"\"\n    print(\"To restore MacPorts shell configuration:\")\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    print(f\"Add these lines to your shell configuration file:\")\n    print(f\"export PATH={prefix}/bin:{prefix}/sbin:$PATH\")\n    print(f\"export MANPATH={prefix}/share/man:$MANPATH\")"
        },
        "uninstall": {
          "filename": "Darwin/Decommission/uninstall.star",
          "content": "# macports/Darwin/Decommission/uninstall.star - Uninstall phase for MacPorts removal\n#\n# TRIBAL KNOWLEDGE:\n# MacPorts doesn't provide an official uninstaller, so removal requires manually\n# uninstalling all ports first, then removing the MacPorts directories.\n# The order is important: ports first, then MacPorts base system.\n\ndef uninstall():\n    \"\"\"Remove all MacPorts installations and the base system.\"\"\"\n    _uninstall_all_ports()\n    _remove_macports_base()\n    return {\"uninstall\": True}\n\ndef _uninstall_all_ports():\n    \"\"\"Remove all installed MacPorts ports.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    port_cmd = f\"{prefix}/bin/port\"\n    \n    if not fs.exists(port_cmd):\n        print(\"MacPorts not found - skipping port removal\")\n        return\n    \n    # Get list of installed ports\n    print(\"Getting list of installed ports...\")\n    result = shell.exec(\n        f\"{port_cmd} installed\",\n        allowed_commands=[port_cmd]\n    )\n    \n    if result.success:\n        installed_lines = [line for line in result.stdout.split(\"\\n\")\n                          if line.strip() and \"@\" in line]\n        \n        if installed_lines:\n            print(f\"Found {len(installed_lines)} installed ports\")\n            \n            # Create backup before removal\n            backup_file = \"/tmp/macports-removed-ports.txt\"\n            shell.exec(\n                f\"{port_cmd} -qv installed > {backup_file}\",\n                allowed_commands=[port_cmd]\n            )\n            print(f\"Port list backed up to {backup_file}\")\n            \n            # Uninstall all ports\n            print(\"Uninstalling all ports (this may take a while)...\")\n            result = shell.exec(\n                f\"sudo {port_cmd} -f uninstall installed\",\n                allowed_commands=[port_cmd]\n            )\n            \n            if result.success:\n                print(\"All ports uninstalled successfully\")\n            else:\n                print(\"WARNING: Some ports may not have uninstalled cleanly\")\n                print(\"Continuing with MacPorts base removal...\")\n        else:\n            print(\"No ports installed\")\n    else:\n        print(\"Could not get port list - continuing with base removal\")\n\ndef _remove_macports_base():\n    \"\"\"Remove the MacPorts base system.\"\"\"\n    prefix = env.get(\"prefix\", \"/opt/local\")\n    \n    print(\"Removing MacPorts base system...\")\n    \n    # Main installation directory\n    if fs.exists(prefix):\n        print(f\"Removing {prefix}...\")\n        shell.exec(\n            f\"sudo rm -rf {prefix}\",\n            allowed_commands=[\"rm\"]\n        )\n    \n    # Application directories\n    app_dirs = [\n        \"/Applications/DarwinPorts\",\n        \"/Applications/MacPorts\"\n    ]\n    \n    for app_dir in app_dirs:\n        if fs.exists(app_dir):\n            print(f\"Removing {app_dir}...\")\n            shell.exec(\n                f\"sudo rm -rf {app_dir}\",\n                allowed_commands=[\"rm\"]\n            )\n    \n    # Package receipts (older systems)\n    receipt_patterns = [\n        \"/Library/Receipts/DarwinPorts*.pkg\",\n        \"/Library/Receipts/MacPorts*.pkg\"\n    ]\n    \n    for pattern in receipt_patterns:\n        result = shell.exec(\n            f\"ls {pattern} 2>/dev/null\",\n            allowed_commands=[\"ls\"]\n        )\n        \n        if result.success and result.stdout.strip():\n            print(f\"Removing receipt files: {pattern}\")\n            shell.exec(\n                f\"sudo rm -rf {pattern}\",\n                allowed_commands=[\"rm\"]\n            )\n    \n    # Modern package receipts\n    receipt_ids = [\n        \"org.macports.MacPorts\",\n        \"org.darwinports.DarwinPorts\"\n    ]\n    \n    for receipt_id in receipt_ids:\n        result = shell.exec(\n            f\"pkgutil --pkgs | grep {receipt_id}\",\n            allowed_commands=[\"pkgutil\", \"grep\"]\n        )\n        \n        if result.success and result.stdout.strip():\n            print(f\"Removing package receipt: {receipt_id}\")\n            shell.exec(\n                f\"sudo pkgutil --forget {receipt_id}\",\n                allowed_commands=[\"pkgutil\"]\n            )\n    \n    print(\"MacPorts base system removed\")\n\ndef rollback():\n    \"\"\"Rollback is not practical for uninstall phase.\"\"\"\n    print(\"Cannot rollback uninstall - MacPorts would need to be reinstalled\")\n    \n    backup_file = \"/tmp/macports-removed-ports.txt\"\n    if fs.exists(backup_file):\n        print(f\"Port list backup available at: {backup_file}\")\n        print(\"Use this to reinstall ports after reinstalling MacPorts\")\n    \n    print(\"To reinstall MacPorts:\")\n    print(\"1. Download installer from https://www.macports.org/install.php\")\n    print(\"2. Install MacPorts\")\n    print(\"3. Run: sudo port selfupdate\")\n    print(\"4. Reinstall ports from backup list if available\")"
        },
        "cleanup": {
          "filename": "Darwin/Decommission/cleanup.star",
          "content": "# macports/Darwin/Decommission/cleanup.star - Cleanup phase for MacPorts removal\n\ndef cleanup():\n    \"\"\"Final cleanup of MacPorts remnants.\"\"\"\n    _remove_launchd_items()\n    _remove_users_and_groups()\n    _remove_remaining_files()\n    _cleanup_temp_files()\n    return {\"cleanup\": True}\n\ndef _remove_launchd_items():\n    \"\"\"Remove any LaunchDaemons or LaunchAgents.\"\"\"\n    launchd_locations = [\n        \"/Library/LaunchDaemons\",\n        \"/Library/LaunchAgents\", \n        \"/System/Library/LaunchDaemons\",\n        \"/System/Library/LaunchAgents\"\n    ]\n    \n    patterns = [\"*macports*\", \"*darwinports*\"]\n    \n    for location in launchd_locations:\n        if not fs.exists(location):\n            continue\n            \n        for pattern in patterns:\n            result = shell.exec(\n                f\"ls {location}/{pattern} 2>/dev/null\",\n                allowed_commands=[\"ls\"]\n            
