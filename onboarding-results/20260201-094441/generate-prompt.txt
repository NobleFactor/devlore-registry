You are a package manifest generator for the devlore package management system. Given extracted slots (from document parsing or user conversation), generate a complete lore package.

## Your Role

Transform structured slot data into a valid lore package consisting of:
- `lifecycle.yaml` — Package metadata, pipelines, features, settings (platform-agnostic)
- `README.md` — Documentation with tribal knowledge
- Platform directories containing phase scripts (composed into pipelines)

## Pipelines and Phases

Lore packages define **pipelines** that orchestrate **phases**. Phases are reusable building blocks; pipelines compose them for specific operations.

### Pipelines

| Pipeline | Purpose | Default Phases |
|----------|---------|----------------|
| Deploy | Fresh installation | prepare → install → provision → verify |
| Upgrade | Version upgrade, OS migration | prepare → upgrade → provision → verify |
| Reconcile | Detect/fix drift from desired state | verify → reconcile → verify |
| Decommission | Graceful removal with cleanup | prepare → decommission → cleanup → verify |

### Phases

| Phase | Purpose | Used By |
|-------|---------|---------|
| `prepare` | Precondition checks, conflict detection | Deploy, Upgrade, Decommission |
| `install` | Software acquisition | Deploy |
| `upgrade` | Version upgrade logic (selfupdate, migrate) | Upgrade |
| `provision` | Post-install/upgrade configuration | Deploy, Upgrade |
| `verify` | State verification | All pipelines |
| `reconcile` | Drift detection and correction | Reconcile |
| `decommission` | Graceful service/process shutdown | Decommission |
| `cleanup` | Remove directories, users, configs | Decommission |

### Rollback

Each phase has a `rollback()` function called by the engine when:
1. A subsequent phase fails (reverse order rollback)
2. User runs `lore decommission <package>` (Decommission pipeline preferred, but rollback as fallback)

## Package Structure

Platform-specific scripts go in platform directories:

```
packages/<name>/
  lifecycle.yaml              # Declares pipelines, phases, features, settings
  README.md                   # Documentation
  Darwin/                     # macOS-specific scripts
    prepare.star              # Precondition checks
    install.star              # Software acquisition (Deploy)
    upgrade.star              # Version upgrade (Upgrade)
    provision.star            # Configuration (Deploy, Upgrade)
    verify.star               # State verification (all)
    reconcile.star            # Drift correction (Reconcile)
    decommission.star         # Graceful shutdown (Decommission)
    cleanup.star              # Final removal (Decommission)
  Linux/                      # Linux-specific scripts (if supported)
    ...
  Windows/                    # Windows-specific scripts (if supported)
    ...
```

**Key principle:** Each platform gets its own directory. Scripts within a platform directory don't need `if platform.os == "..."` checks — the directory structure handles platform dispatch.

**Minimum viable package:** At minimum, generate the Deploy pipeline phases (prepare, install, provision, verify). Add other phases when the slots contain upgrade, reconciliation, or decommission information.

**Single-platform packages:** If a package only supports one platform (e.g., MacPorts is Darwin-only), only include that platform's directory.

## Generation Rules

### lifecycle.yaml

```yaml
name: macports
version: "2.11.6"
description: Package manager for macOS
homepage: https://www.macports.org
license: BSD-3-Clause

platforms:
  - darwin

pipelines:
  deploy:
    phases: [prepare, install, provision, verify]
  upgrade:
    phases: [prepare, upgrade, provision, verify]
  decommission:
    phases: [prepare, decommission, cleanup, verify]
  reconcile:
    phases: [verify, reconcile, verify]

features:
  trace-mode:
    description: Enable trace mode to avoid /usr/local interference
    default: false

settings:
  macos-version:
    type: string
    default: "auto"
    description: Target macOS version for installer selection

dependencies:
  - name: xcode-command-line-tools
    required: true

conflicts:
  - name: homebrew
    reason: Different installation prefixes can cause library conflicts

verification:
  command: "port version"
  pattern: "Version: \\d+\\.\\d+\\.\\d+"

tags:
  - package-manager
  - macos
```

### Phase Scripts (Starlark)

1. Every script has a main function matching the phase name and a `rollback()` function
2. Scripts are platform-specific — no `if platform.os` checks needed
3. Use `platform.arch` for architecture-specific logic (e.g., arm64 vs x86_64)
4. Use `package.install()` for package manager installs (not raw shell commands)
5. Use `shell.exec()` only for operations without a dedicated API
6. All `shell.exec()` calls must include `allowed_commands=[...]`
7. Include TRIBAL KNOWLEDGE comments documenting why non-obvious steps exist
8. Return a dict with the phase name as key and success boolean as value

### Available Host Bindings

| Namespace | Key Functions |
|-----------|---------------|
| `platform.*` | `.os`, `.arch`, `.distro`, `.version`, `.version_gte()` |
| `package.*` | `.install()`, `.remove()`, `.installed()`, `.version()` |
| `fs.*` | `.exists()`, `.read()`, `.write()`, `.mkdir()`, `.remove()`, `.which()` |
| `shell.*` | `.exec(command, allowed_commands=[...])` |
| `http.*` | `.download()`, `.get()`, `.fetch_json()` |
| `archive.*` | `.extract()`, `.list()` |
| `env.*` | `.get()`, `.set()`, `.expand()` |
| `service.*` | `.enable()`, `.start()`, `.stop()`, `.status()` |

### README.md

1. Start with a one-paragraph description
2. Include a "Pipelines" section explaining what each pipeline does for this package
3. Include a "Tribal Knowledge" section documenting gotchas
4. Document all features and settings in tables
5. List source URLs consulted
6. Reference specific file locations for tribal knowledge (e.g., `Darwin/upgrade.star:28-51`)

## Quality Checks

Before outputting, verify:
- [ ] lifecycle.yaml is valid YAML with pipelines declared
- [ ] All phase scripts referenced by pipelines exist
- [ ] Each phase script has main function and rollback()
- [ ] No shell.exec() without allowed_commands
- [ ] Verification command actually tests functionality (not just existence)
- [ ] README documents pipelines, features, and settings
- [ ] Tribal knowledge has source references

## Output Format

Respond with the complete file contents for each file in the package, using the package-output schema structure. Include at minimum the Deploy pipeline phases. Include Upgrade, Reconcile, and Decommission phases when the source documentation provides relevant information.

# === INSTALL TYPE PATTERNS ===

```yaml
# Install Types - Patterns for software installation across platforms
# These are the building blocks for lore package install phases

install_types:

  script_install:
    name: "Script-based Installation"
    description: "Download and execute an installation script (curl | sh pattern)"
    platforms: [linux, darwin]
    examples:
      - homebrew_linux
      - ollama_linux
    pattern: |
      curl -fsSL <script_url> | sh
    considerations:
      - Script should be auditable (provide source URL)
      - May require sudo for system-wide install
      - Often creates users, groups, services
      - Idempotent scripts can be re-run safely
    starlark_pattern: |
      def install_via_script(script_url):
          """Install using a remote script."""
          shell.exec(
              f"curl -fsSL {script_url} | sh",
              allowed_commands=["curl", "sh"],
          )
    rollback_pattern: |
      # Many scripts support --uninstall flag
      # Or require manual cleanup of created resources
    tribal_knowledge:
      - "Always provide --uninstall or cleanup instructions"
      - "Scripts may modify shell profiles (.bashrc, .zshrc)"
      - "User may need to restart shell or source profile"

  pkg_installer:
    name: "macOS Package Installer"
    description: "Standard macOS .pkg installer executed via Installer.app or installer CLI"
    platforms: [darwin]
    examples:
      - macports
      - homebrew_pkg
    pattern: |
      sudo installer -pkg <path_to_pkg> -target /
    considerations:
      - Requires downloading .pkg first
      - Version-specific packages (must match macOS version)
      - Can be silent with -pkg and -target flags
      - Receipts stored in /var/db/receipts/
    starlark_pattern: |
      def install_via_pkg(pkg_url, pkg_name):
          """Install using macOS .pkg installer."""
          temp_path = f"/tmp/{pkg_name}"
          http.download(pkg_url, temp_path)
          shell.exec(
              f"sudo installer -pkg {temp_path} -target /",
              allowed_commands=["installer"],
          )
          fs.remove(temp_path)
    rollback_pattern: |
      # PKG installers don't have standard uninstall
      # Must track installed files via receipts or documentation
      # pkgutil --pkgs | grep <identifier>
      # pkgutil --files <identifier>
    tribal_knowledge:
      - "Check pkgutil --pkgs for installed package receipts"
      - "Some packages provide uninstall scripts"
      - "Files may be scattered across /usr/local, /opt, /Library"

  dmg_app_install:
    name: "macOS DMG Application"
    description: "Mount DMG, copy .app to /Applications"
    platforms: [darwin]
    examples:
      - ollama_macos
    pattern: |
      hdiutil attach <dmg_path>
      cp -R /Volumes/<name>/<app>.app /Applications/
      hdiutil detach /Volumes/<name>
    considerations:
      - Mount point varies by DMG
      - Some apps require first-run setup
      - May install helper tools or CLI symlinks
    starlark_pattern: |
      def install_via_dmg(dmg_url, app_name, volume_name):
          """Install macOS app from DMG."""
          temp_dmg = f"/tmp/{app_name}.dmg"
          http.download(dmg_url, temp_dmg)

          # Mount DMG
          result = shell.exec(
              f"hdiutil attach {temp_dmg} -nobrowse",
              allowed_commands=["hdiutil"],
          )
          mount_point = f"/Volumes/{volume_name}"

          # Copy app
          shell.exec(
              f"cp -R {mount_point}/{app_name}.app /Applications/",
              allowed_commands=["cp"],
          )

          # Cleanup
          shell.exec(f"hdiutil detach {mount_point}", allowed_commands=["hdiutil"])
          fs.remove(temp_dmg)
    rollback_pattern: |
      # Simply remove the .app bundle
      rm -rf /Applications/<app_name>.app
      # May also need to remove:
      # ~/Library/Application Support/<app_name>
      # ~/Library/Preferences/com.<vendor>.<app>.plist
      # ~/Library/Caches/<app_name>
    tribal_knowledge:
      - "Use -nobrowse flag to prevent Finder from opening"
      - "Some DMGs are code-signed and require verification"
      - "First launch may trigger Gatekeeper approval"

  exe_installer:
    name: "Windows EXE/MSI Installer"
    description: "Standard Windows installer with silent install options"
    platforms: [windows]
    examples:
      - ollama_windows
    pattern: |
      <installer>.exe /S          # NSIS-style silent
      msiexec /i <installer>.msi /qn  # MSI silent
    considerations:
      - Silent flags vary by installer type (/S, /silent, /quiet, /qn)
      - May require elevated privileges
      - Uninstall via registry or Add/Remove Programs
    starlark_pattern: |
      def install_via_exe(exe_url, exe_name, silent_flag="/S"):
          """Install using Windows EXE installer."""
          temp_path = env.expand("%TEMP%") + f"\\{exe_name}"
          http.download(exe_url, temp_path)
          shell.exec(
              f"{temp_path} {silent_flag}",
              allowed_commands=[temp_path],
          )
          fs.remove(temp_path)
    rollback_pattern: |
      # Query registry for uninstall string:
      # HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall
      # Or use: wmic product where name="<name>" call uninstall
    tribal_knowledge:
      - "NSIS installers use /S, InnoSetup uses /SILENT or /VERYSILENT"
      - "MSI uses msiexec with /qn for quiet, no UI"
      - "Some installers require reboot (/NORESTART to suppress)"

  source_build:
    name: "Source Compilation"
    description: "Build from source using configure/make or similar"
    platforms: [darwin, linux]
    examples:
      - macports_source
    pattern: |
      ./configure [--prefix=<path>]
      make
      sudo make install
    considerations:
      - Requires build tools (gcc/clang, make)
      - May need additional dependencies
      - Prefix determines install location
    starlark_pattern: |
      def install_from_source(tarball_url, name, version, configure_args=""):
          """Build and install from source tarball."""
          temp_dir = f"/tmp/{name}-{version}"
          tarball = f"{temp_dir}.tar.gz"

          http.download(tarball_url, tarball)
          archive.extract(tarball, "/tmp")

          shell.exec(
              f"cd {temp_dir} && ./configure {configure_args}",
              allowed_commands=["configure"],
          )
          shell.exec(f"cd {temp_dir} && make", allowed_commands=["make"])
          shell.exec(f"cd {temp_dir} && sudo make install", allowed_commands=["make"])

          fs.remove(temp_dir)
          fs.remove(tarball)
    rollback_pattern: |
      # If Makefile has uninstall target:
      sudo make uninstall
      # Otherwise manual removal based on --prefix
    tribal_knowledge:
      - "Always check ./configure --help for options"
      - "Use --prefix to install to non-standard location"
      - "make -j$(nproc) for parallel builds"

  selfupdate:
    name: "Self-updating Package Manager"
    description: "Package manager that can update itself"
    platforms: [darwin, linux]
    examples:
      - macports
      - homebrew
    considerations:
      - Critical after OS upgrades
      - May require migration steps
      - Can fail if base is too old
    starlark_pattern: |
      def selfupdate(command):
          """Update the package manager itself."""
          shell.exec(command, allowed_commands=[command.split()[0]])
    tribal_knowledge:
      - "Run selfupdate before installing new packages"
      - "After OS upgrade, selfupdate may fail - reinstall base"
      - "Keep package manager current to avoid dependency issues"

os_upgrade_patterns:
  description: "Patterns for handling package managers after OS upgrades"

  homebrew:
    steps:
      - "Update Xcode Command Line Tools: xcode-select --install"
      - "Run: brew update"
      - "Run: brew upgrade"
      - "Fix permissions if needed: sudo chown -R $(whoami) $(brew --prefix)/*"
    common_issues:
      - "Xcode license not accepted: sudo xcodebuild -license accept"
      - "Broken bottles: brew reinstall <package>"

  macports:
    steps:
      - "Update Xcode and Command Line Tools"
      - "Accept Xcode license: sudo xcodebuild -license accept"
      - "Review /opt/local/etc/macports/macports.conf"
      - "Run: sudo port migrate"
      - "Or reinstall MacPorts base from .pkg for current OS"
    common_issues:
      - "Ports built with old compiler won't work"
      - "Registry permissions: sudo port outdated"
      - "CLT headers missing: remove legacy c++ directory"
    migration_command: "sudo port migrate"
    full_reinstall:
      - "Export port list: port -qv installed > myports.txt"
      - "Uninstall all: sudo port -f uninstall installed"
      - "Remove MacPorts: sudo rm -rf /opt/local"
      - "Install new .pkg for current OS"
      - "Restore: sudo port install $(cat myports.txt | awk '{print $1}')"
```

# === EXAMPLES ===

# Few-shot examples for package generation
#
# Each example shows: parsed slots → generated package files

examples:
  - name: "Simple CLI tool (ripgrep)"
    input_slots:
      - { category: identity, name: package_name, value: ripgrep }
      - { category: identity, name: version, value: "14.1.0" }
      - { category: identity, name: description, value: "Fast regex search tool" }
      - { category: identity, name: homepage, value: "https://github.com/BurntSushi/ripgrep" }
      - { category: identity, name: license, value: "MIT" }
      - { category: platform, name: platforms, value: [darwin, linux, windows] }
      - { category: installation, name: install_command, value: "brew install ripgrep", platform: darwin }
      - { category: installation, name: install_command, value: "apt install ripgrep", platform: linux }
      - { category: installation, name: install_command, value: "winget install BurntSushi.ripgrep.MSVC", platform: windows }
      - { category: verification, name: verify_command, value: "rg --version" }
      - { category: verification, name: verify_pattern, value: "ripgrep \\d+\\.\\d+\\.\\d+" }
      - { category: feature, name: shell_completions, value: { description: "Install shell completions", default: true } }

    output_lifecycle: |
      name: ripgrep
      version: "14.1.0"
      description: Fast regex search tool
      homepage: https://github.com/BurntSushi/ripgrep
      repository: https://github.com/BurntSushi/ripgrep
      license: MIT

      authors:
        - name: Andrew Gallant
          url: https://github.com/BurntSushi

      platforms:
        - darwin
        - linux
        - windows

      features:
        shell-completions:
          description: Install shell completions for bash, zsh, fish
          default: true

      settings: {}

      dependencies: []
      conflicts: []

      phases:
        prepare: prepare.star
        install: install.star
        provision: provision.star
        verify: verify.star

      verification:
        command: "rg --version"
        pattern: "ripgrep \\d+\\.\\d+\\.\\d+"

      tags:
        - cli
        - search
        - rust

    output_install: |
      # ripgrep/install.star - Install phase for ripgrep

      def install():
          """Install ripgrep."""
          if platform.os == "darwin":
              _install_darwin()
          elif platform.os == "linux":
              _install_linux()
          elif platform.os == "windows":
              _install_windows()
          return {"install": True}

      def _install_darwin():
          """Install on macOS via Homebrew."""
          package.install("ripgrep", manager="brew")

      def _install_linux():
          """Install on Linux via apt."""
          package.install("ripgrep", manager="apt")

      def _install_windows():
          """Install on Windows via winget."""
          package.install("BurntSushi.ripgrep.MSVC", manager="winget")

      def rollback():
          """Remove ripgrep."""
          if platform.os == "darwin":
              package.remove("ripgrep", manager="brew")
          elif platform.os == "linux":
              package.remove("ripgrep", manager="apt")
          elif platform.os == "windows":
              package.remove("BurntSushi.ripgrep.MSVC", manager="winget")

  - name: "Complex package with tribal knowledge (Docker)"
    input_slots:
      - { category: identity, name: package_name, value: docker }
      - { category: identity, name: version, value: "27.4.0" }
      - { category: platform, name: platforms, value: [darwin, linux] }
      - { category: conflict, name: conflicting_packages, value: ["docker.io", "docker-engine", "containerd", "runc"], platform: linux }
      - { category: installation, name: install_command, value: "brew install --cask docker", platform: darwin }
      - { category: installation, name: install_packages, value: ["docker-ce", "docker-ce-cli", "containerd.io", "docker-buildx-plugin", "docker-compose-plugin"], platform: linux }
      - { category: configuration, name: post_install, value: "usermod -aG docker $USER", platform: linux }
      - { category: verification, name: verify_command, value: "docker version" }
      - { category: feature, name: compose, value: { description: "Docker Compose plugin", default: true } }
      - { category: feature, name: buildx, value: { description: "Docker Buildx for multi-platform builds", default: true } }

    output_prepare: |
      # docker/prepare.star - Prepare phase for Docker
      #
      # TRIBAL KNOWLEDGE:
      # Legacy Docker packages (docker.io, docker-engine) conflict with Docker CE.
      # They must be completely removed before installation or Docker CE will
      # fail silently or produce version conflicts.

      LEGACY_PACKAGES = ["docker.io", "docker-engine", "docker", "containerd", "runc"]

      def prepare():
          """Check preconditions and remove conflicting packages."""
          if platform.os == "darwin":
              _prepare_darwin()
          elif platform.os == "linux":
              _prepare_linux()
          return {"prepare": True}

      def _prepare_darwin():
          """Check macOS preconditions."""
          # Docker Desktop requires macOS 12+ on Apple Silicon
          if platform.arch == "arm64":
              if not platform.version_gte("12.0"):
                  fail("Docker Desktop requires macOS 12+ on Apple Silicon")

      def _prepare_linux():
          """Remove legacy Docker packages on Linux."""
          for pkg in LEGACY_PACKAGES:
              if package.installed(pkg):
                  package.remove(pkg, manager="apt")

      def rollback():
          """No rollback needed for prepare."""
          pass

    output_provision: |
      # docker/provision.star - Provision phase for Docker
      #
      # TRIBAL KNOWLEDGE:
      # Without adding the user to the 'docker' group, every docker command
      # requires sudo. This is the #1 complaint from new Docker users.
      # The group change requires a new login session to take effect,
      # but `newgrp docker` activates it immediately.

      def provision():
          """Configure Docker for use."""
          if platform.os == "darwin":
              _provision_darwin()
          elif platform.os == "linux":
              _provision_linux()
          return {"provision": True}

      def _provision_darwin():
          """macOS: Docker Desktop handles its own provisioning."""
          pass

      def _provision_linux():
          """Linux: Add user to docker group for rootless usage."""
          shell.exec(
              "sudo usermod -aG docker $USER",
              allowed_commands=["usermod"],
          )
          # Enable and start Docker daemon
          service.enable("docker")
          service.start("docker")

      def rollback():
          """Remove docker group membership."""
          if platform.os == "linux":
              shell.exec(
                  "sudo gpasswd -d $USER docker",
                  allowed_commands=["gpasswd"],
              )

# === OUTPUT SCHEMA ===

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PackageOutput",
  "description": "Structured output for a generated lore package with pipelines, phases, and platform-specific scripts",
  "type": "object",
  "required": ["package_name", "files", "platforms"],
  "properties": {
    "package_name": {
      "type": "string",
      "description": "Package name (lowercase, hyphenated)",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "files": {
      "type": "object",
      "description": "Platform-agnostic files at package root",
      "required": ["lifecycle", "readme"],
      "properties": {
        "lifecycle": {
          "$ref": "#/$defs/FileContent",
          "description": "lifecycle.yaml - package metadata, pipelines, features, settings"
        },
        "readme": {
          "$ref": "#/$defs/FileContent",
          "description": "README.md - documentation with tribal knowledge"
        }
      }
    },
    "platforms": {
      "type": "object",
      "description": "Platform-specific phase scripts. Keys are platform names: Darwin, Linux, Windows",
      "additionalProperties": {
        "$ref": "#/$defs/PlatformPhases"
      }
    },
    "tribal_knowledge_summary": {
      "type": "array",
      "description": "Key insights encoded in the package",
      "items": {
        "type": "object",
        "required": ["title", "file", "lines"],
        "properties": {
          "title": {
            "type": "string",
            "description": "Name of the insight"
          },
          "file": {
            "type": "string",
            "description": "Which file implements it (e.g., 'Darwin/install.star')"
          },
          "lines": {
            "type": "string",
            "description": "Line range (e.g., '28-51')"
          }
        }
      }
    },
    "sources_consulted": {
      "type": "array",
      "description": "URLs and documents used to create this package",
      "items": {
        "type": "object",
        "required": ["title", "relevance"],
        "properties": {
          "title": { "type": "string" },
          "url": { "type": "string", "format": "uri" },
          "relevance": { "type": "string" }
        }
      }
    }
  },
  "$defs": {
    "FileContent": {
      "type": "object",
      "required": ["filename", "content"],
      "properties": {
        "filename": {
          "type": "string",
          "description": "Filename (e.g., 'lifecycle.yaml', 'Darwin/install.star')"
        },
        "content": {
          "type": "string",
          "description": "Complete file content"
        }
      }
    },
    "PlatformPhases": {
      "type": "object",
      "description": "Phase scripts for a specific platform. Phases are composed into pipelines.",
      "required": ["prepare", "install", "provision", "verify"],
      "properties": {
        "prepare": {
          "$ref": "#/$defs/FileContent",
          "description": "prepare.star - precondition checks (used by Deploy, Upgrade, Decommission)"
        },
        "install": {
          "$ref": "#/$defs/FileContent",
          "description": "install.star - software acquisition (Deploy pipeline)"
        },
        "provision": {
          "$ref": "#/$defs/FileContent",
          "description": "provision.star - post-install configuration (Deploy, Upgrade pipelines)"
        },
        "verify": {
          "$ref": "#/$defs/FileContent",
          "description": "verify.star - state verification (all pipelines)"
        },
        "upgrade": {
          "$ref": "#/$defs/FileContent",
          "description": "upgrade.star - version upgrade logic (Upgrade pipeline)"
        },
        "reconcile": {
          "$ref": "#/$defs/FileContent",
          "description": "reconcile.star - drift detection and correction (Reconcile pipeline)"
        },
        "decommission": {
          "$ref": "#/$defs/FileContent",
          "description": "decommission.star - graceful removal (Decommission pipeline)"
        },
        "cleanup": {
          "$ref": "#/$defs/FileContent",
          "description": "cleanup.star - final cleanup of dirs, users, configs (Decommission pipeline)"
        }
      }
    }
  }
}
```

# === EXTRACTED SLOTS ===

Generate a lore package from these extracted slots:
```yaml
```yaml
package_name: macports

slots:
  - category: identity
    name: name
    value: MacPorts
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - title"

  - category: identity
    name: version
    value: "2.11.6"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Installing MacPorts section"

  - category: identity
    name: description
    value: "An open-source community initiative to design an easy-to-use system for compiling, installing, and upgrading either command-line, X11 or Aqua based open-source software on the Mac OS X operating system"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - meta description"

  - category: identity
    name: homepage
    value: "https://www.macports.org/"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - site header"

  - category: platform
    name: supported_os
    value:
      - "macOS Tahoe v26"
      - "macOS Sequoia v15"
      - "macOS Sonoma v14"
      - "macOS Ventura v13"
      - "macOS Monterey v12"
      - "macOS Big Sur v11"
      - "macOS Catalina v10.15"
      - "macOS Mojave v10.14"
      - "macOS High Sierra v10.13"
      - "macOS Sierra v10.12"
      - "OS X El Capitan v10.11"
      - "OS X Yosemite v10.10"
      - "OS X Mavericks v10.9"
      - "OS X Mountain Lion v10.8"
      - "OS X Lion v10.7"
      - "Mac OS X Snow Leopard v10.6"
      - "Mac OS X Leopard v10.5"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Available Downloads section"

  - category: dependency
    name: required_packages
    value:
      - "Apple Command Line Developer Tools"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Quickstart"

  - category: dependency
    name: install_command
    value: "xcode-select --install"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Quickstart"
    annotation: "Required before MacPorts installation"

  - category: dependency
    name: optional_packages
    value:
      - "Apple Xcode Developer Tools"
      - "X11 windowing environment"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Requirements section"

  - category: dependency
    name: xcode_versions
    value:
      darwin_tahoe: "26.0 or later"
      darwin_sequoia: "16.0 or later"
      darwin_sonoma: "15.0 or later"
      darwin_ventura: "14.1 or later"
      darwin_monterey: "13.1 or later"
      darwin_big_sur: "12.2 or later"
      darwin_catalina: "11.3 or later"
      darwin_mojave: "10.0 or later"
      darwin_high_sierra: "9.0 or later"
      darwin_sierra: "8.0 or later"
      darwin_el_capitan: "7.0 or later"
      darwin_yosemite: "6.1 or later"
      darwin_mavericks: "5.0.1 or later"
      darwin_mountain_lion: "4.4 or later"
      darwin_lion: "4.1 or later"
      darwin_snow_leopard: "3.2 or later"
      darwin_leopard: "3.1 or later"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Requirements section"

  - category: installation
    name: pkg_installers
    value:
      - "MacPorts-2.11.6-26-Tahoe.pkg"
      - "MacPorts-2.11.6-15-Sequoia.pkg"
      - "MacPorts-2.11.6-14-Sonoma.pkg"
      - "MacPorts-2.11.6-13-Ventura.pkg"
      - "MacPorts-2.11.6-12-Monterey.pkg"
      - "MacPorts-2.11.6-11-BigSur.pkg"
      - "MacPorts-2.11.6-10.15-Catalina.pkg"
      - "MacPorts-2.11.6-10.14-Mojave.pkg"
      - "MacPorts-2.11.6-10.13-HighSierra.pkg"
      - "MacPorts-2.11.6-10.12-Sierra.pkg"
    confidence: 1.0
    platform: darwin
    source_ref: "guide.macports.org - macOS Package Install section"
    annotation: "Simplest installation method for most users"

  - category: installation
    name: source_download
    value:
      - "MacPorts-2.11.6.tar.bz2"
      - "MacPorts-2.11.6.tar.gz"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Installing MacPorts section"

  - category: installation
    name: source_install_commands
    value:
      - "tar xjvf MacPorts-2.11.6.tar.bz2"
      - "cd MacPorts-2.11.6"
      - "./configure && make && sudo make install"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Source Installation section"

  - category: installation
    name: git_repository
    value: "https://github.com/macports/macports-base.git"
    confidence: 1.0
    platform: all
    source_ref: "guide.macports.org - Git Install section"

  - category: installation
    name: checksums_file
    value: "https://github.com/macports/macports-base/releases/download/v2.11.6/MacPorts-2.11.6.chk.txt"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Installing MacPorts section"

  - category: configuration
    name: shell_environment_setup
    value:
      - "Add ${prefix}/bin and ${prefix}/sbin to the start of PATH"
      - "Add ${prefix}/share/man to MANPATH if needed"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Source Installation section"
    annotation: "Required for source installation, automatic for pkg installer"

  - category: configuration
    name: xcode_license_acceptance
    value: "xcodebuild -license"
    confidence: 1.0
    platform: darwin
    source_ref: "macports.org/install.php - Requirements section"
    annotation: "Required for Xcode 4 and later users"

  - category: configuration
    name: selfupdate_command
    value: "sudo port -v selfupdate"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - macOS Package (.pkg) Installer section"
    annotation: "Run regularly to keep MacPorts current"

  - category: verification
    name: verify_command
    value: "port version"
    confidence: 1.0
    platform: all
    source_ref: "guide.macports.org - macOS Package Install section"

  - category: verification
    name: expected_output
    value: "Version: 2.11.6"
    confidence: 1.0
    platform: all
    source_ref: "guide.macports.org - macOS Package Install section"

  - category: verification
    name: help_command
    value: "man port"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - macOS Package (.pkg) Installer section"

  - category: feature
    name: migration_support
    value: "port migrate"
    confidence: 1.0
    platform: all
    source_ref: "trac.macports.org/wiki/Migration - Migration procedure section"
    annotation: "For major OS upgrades or architecture changes, requires MacPorts 2.10.0+"

  - category: feature
    name: upgrade_method
    value: "sudo port selfupdate"
    confidence: 1.0
    platform: all
    source_ref: "macports.org/install.php - Selfupdate section"
    annotation: "Uses rsync networking protocol (tcp port 873)"

  - category: setting
    name: default_prefix
    value: "/opt/local"
    confidence: 1.0
    platform: all
    source_ref: "guide.macports.org - Source Install section"

  - category: setting
    name: config_file
    value: "/opt/local/etc/macports/macports.conf"
    confidence: 1.0
    platform: all
    source_ref: "trac.macports.org/wiki/Migration - Review macports.conf settings"

sources:
  - title: "macports.org/install.php"
    url: "https://www.macports.org/install.php"
    trust_level: "vendor"
    sections_used:
      - "Quickstart"
      - "Installing MacPorts"
      - "macOS Package (.pkg) Installer"
      - "Source Installation"
      - "Requirements"

  - title: "trac.macports.org/wiki/Migration"
    trust_level: "vendor"
    sections_used:
      - "Migration procedure"
      - "Review macports.conf settings"

  - title: "guide.macports.org"
    trust_level: "vendor"
    sections_used:
      - "macOS Package Install"
      - "Source Install"
      - "Git Install"

ambiguities:
  - slot_name: "x11_requirement"
    description: "X11 windowing environment is optional but multiple choices available"
    options:
      - "Install xorg-server port from MacPorts (recommended)"
      - "Use XQuartz Project X11 release"
      - "Use Apple's X11.app (older OS versions)"
    suggested_default: "xorg-server port from MacPorts"

tribal_knowledge:
  - title: "Xcode License Must Be Accepted"
    description: "With Xcode 4 and later, users need to accept the Xcode EULA by either launching Xcode or running xcodebuild -license"
    source_ref: "macports.org/install.php - Requirements section"
    affects_phase: "prepare"

  - title: "Command Line Tools Are Critical"
    description: "Apple's Command Line Developer Tools are required and must be installed via xcode-select --install. Some ports require full Xcode but most only need CLT"
    source_ref: "macports.org/install.php - Requirements section"
    affects_phase: "prepare"

  - title: "Migration Required for Major OS Upgrades"
    description: "A MacPorts installation is designed to work with a particular operating system and hardware architecture. Migration procedure ensures smooth transition after major system changes"
    source_ref: "trac.macports.org/wiki/Migration"
    affects_phase: "install"

  - title: "Shell Environment Setup Is Automatic for PKG"
    description: "The pkg installer automatically adapts shell configuration files but you may need to open a new shell for changes to take effect. Source installs require manual environment setup"
    source_ref: "macports.org/install.php - macOS Package (.pkg) Installer section"
    affects_phase: "provision"

  - title: "Regular Updates Are Essential"
    description: "The selfupdate command should be run manually on a regular basis to keep your MacPorts system current with latest available release and Portfiles revisions"
    source_ref: "macports.org/install.php - macOS Package (.pkg) Installer section"
    affects_phase: "verify"

  - title: "Architecture Settings May Need Updates"
    description: "If macports.conf contains settings for universal_archs or build_arch, you may need to update them when upgrading OS versions as newer OS versions may support different CPU architectures"
    source_ref: "trac.macports.org/wiki/Migration - Review macports.conf settings"
    affects_phase: "provision"
```
```

# === DISCOVERY CONTEXT ===

Product discovery results:
```json
{
  "product": {
    "name": "MacPorts",
    "canonical_name": "macports",
    "category": "package-manager",
    "vendor": "The MacPorts Project",
    "version": "2.11.6",
    "license": "BSD"
  },
  "sources": {
    "homepage": "https://www.macports.org",
    "installation": ["https://www.macports.org/install.php"],
    "upgrade": ["https://trac.macports.org/wiki/Migration"],
    "uninstall": ["https://guide.macports.org/#installing.macports.uninstalling"],
    "troubleshooting": ["https://trac.macports.org/wiki/ProblemHotlist", "https://guide.macports.org/#installing.macports.troubleshooting"],
    "repository": "https://github.com/macports/macports-base",
    "releases": "https://github.com/macports/macports-base/releases"
  },
  "platforms": {
    "darwin": {
      "supported": true, 
      "architectures": ["arm64", "x86_64"], 
      "notes": "Supports macOS versions from Leopard to Tahoe (v26), version-specific .pkg installers available"
    },
    "linux": {
      "supported": true, 
      "distributions": ["experimental POSIX-compliant platforms"], 
      "notes": "Experimental support, requires source installation, not actively pursued"
    },
    "windows": {
      "supported": false, 
      "min_version": null, 
      "notes": "Not supported, macOS and POSIX platforms only"
    }
  },
  "complexity": {
    "rating": "complex",
    "concerns": [
      "OS upgrade requires migration to new major macOS releases",
      "Version-specific installers for different macOS versions",
      "Xcode Command Line Tools dependency required",
      "Optional Xcode full installation for some ports",
      "X11 environment dependency for some ports",
      "Manual environment configuration needed for source installation",
      "Xcode EULA acceptance required"
    ]
  },
  "next_urls_to_fetch": [
    "https://trac.macports.org/wiki/Migration",
    "https://guide.macports.org",
    "https://trac.macports.org/wiki/ProblemHotlist",
    "https://github.com/macports/macports-base",
    "https://guide.macports.org/#installing.macports.uninstalling",
    "https://guide.macports.org/#installing.macports.troubleshooting"
  ]
}
```
