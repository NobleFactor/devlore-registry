<!-- Auto-generated by: star devlore knowledge extract --domain=onboarding -->
<!-- Do not edit this file manually. -->

# Starlark API Reference

This document describes the bindings available in lore phase scripts.

## Overview

Phase scripts define a function named for the lifecycle phase:

```starlark
def install(package, phase):
    # package - context about the package being deployed
    # phase   - phase context (name, action, retry)
    # plan    - global, graph construction operations
```

| Category | Methods | Properties | Total |
|----------|---------|------------|-------|
| plan.* | 34 | 0 | 34 |
| package.* | 6 | 7 | 13 |
| phase.* | 1 | 2 | 3 |
| **Total** | **41** | **9** | **50** |

---

## plan.*

The `plan` object schedules actions into the execution graph.

| Namespace | Purpose |
|-----------|---------|
| `plan.*` | Top-level plan actions (choose, source, gather) |
| `plan.archive.*` | Archive extraction |
| `plan.content.*` | Content literals |
| `plan.encryption.*` | SOPS decryption |
| `plan.file.*` | File system actions and predicates |
| `plan.git.*` | Git operations and predicates |
| `plan.net.*` | Network operations |
| `plan.package.*` | Package manager actions and predicates |
| `plan.service.*` | Service management actions and predicates |
| `plan.shell.*` | Shell execution |
| `plan.template.*` | Template rendering |

### plan.file

#### plan.file.copy

| Slot | Description |
|------|-------------|
| `source` |  |
| `path` |  |

**Returns:** Output (promise)

#### plan.file.exists

#### plan.file.is_dir

#### plan.file.link

| Slot | Description |
|------|-------------|
| `source` |  |
| `path` |  |

**Returns:** Output (promise)

#### plan.file.remove

| Slot | Description |
|------|-------------|
| `path` |  |

#### plan.file.write

| Slot | Description |
|------|-------------|
| `content` |  |
| `path` |  |

**Returns:** Output (promise)

### plan.package

#### plan.package.install

**Returns:** Output (promise)

#### plan.package.installed

#### plan.package.not_installed

#### plan.package.remove

| Slot | Description |
|------|-------------|
| `path` |  |

#### plan.package.update

**Returns:** Output (promise)

#### plan.package.upgrade

**Returns:** Output (promise)

#### plan.package.version_gte

### plan.service

#### plan.service.disable

#### plan.service.enable

#### plan.service.enabled

#### plan.service.exists

#### plan.service.restart

#### plan.service.running

#### plan.service.start

#### plan.service.stop

### plan.shell

#### plan.shell.exec

| Slot | Description |
|------|-------------|
| `command` |  |

**Returns:** Output (promise)

### plan.net

#### plan.net.download

| Slot | Description |
|------|-------------|
| `url` |  |

**Returns:** Output (promise)

### plan.content

#### plan.content.literal

| Slot | Description |
|------|-------------|
| `content` |  |

**Returns:** Output (promise)

### plan.template

#### plan.template.render

| Slot | Description |
|------|-------------|
| `source` |  |

**Returns:** Output (promise)

### plan.encryption

#### plan.encryption.decrypt

| Slot | Description |
|------|-------------|
| `source` |  |

**Returns:** Output (promise)

### plan.archive

#### plan.archive.extract

| Slot | Description |
|------|-------------|
| `archive` |  |
| `prefix` |  |

**Returns:** Output (promise)

### plan.git

#### plan.git.checkout

| Slot | Description |
|------|-------------|
| `repo` |  |
| `ref` |  |

**Returns:** Output (promise)

#### plan.git.clone

| Slot | Description |
|------|-------------|
| `url` |  |
| `path` |  |

**Returns:** Output (promise)

#### plan.git.installed

#### plan.git.pull

| Slot | Description |
|------|-------------|
| `repo` |  |

**Returns:** Output (promise)

### Top-level plan actions

#### plan.choose

choose creates a conditional branch in the execution graph.

**Usage:** `plan.choose(when=predicate, then=callback)`

**Returns:** Output of the choose node

#### plan.gather

gather creates a handle for parallel execution of multiple nodes.

**Usage:** `plan.gather(promise1, promise2, ...)`

**Returns:** Gather handle that can be passed to other operations

#### plan.source

source creates a source file artifact.

**Usage:** `plan.source(path)`

| Slot | Description |
|------|-------------|
| `path` | Path to existing source file (immediate only) |

**Returns:** Promise of the source file

---

## package.*

The `package` object provides context about the package being deployed.

### Properties

| Property | Type | Description |
|----------|------|-------------|
| `package.dry_run` | bool | True if this is a preview run |
| `package.features` | list[string] | Enabled features |
| `package.name` | string | Package name being deployed |
| `package.settings` | dict[string, string] | Key-value settings |
| `package.source_root` | string | Package source directory |
| `package.target_root` | string | Deployment target directory |
| `package.version` | string | Version being deployed |

### Methods

#### package.has_feature

#### package.install

**Returns:** Output (promise)

#### package.remove

#### package.setting

#### package.update

**Returns:** Output (promise)

#### package.upgrade

**Returns:** Output (promise)

---

## phase.*

The `phase` object is passed as the second argument to lifecycle entry points.

#### phase.retry

retry configures the retry policy for the phase. phase.retry(max_attempts=3, backoff="exponential", initial_delay="1s", max_delay="30s")

---

## Output Objects

Most plan actions return an **Output** object (a promise). When passed to another
action's slot, it creates an edge in the execution graph, ensuring the producer runs
before the consumer.

| Property | Type | Description |
|----------|------|-------------|
| `output.node_id` | string | Unique identifier of the producing node |
| `output.slot` | string | Which output slot this represents |

Use `plan.gather(*promises)` to group multiple outputs for parallel execution.
When the group is passed to another action's slot, it creates edges from all
members to the consumer.
