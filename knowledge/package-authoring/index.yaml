# Auto-generated file list by: go run ./cmd/gen-index
# Metadata (purpose, source_system, description) is preserved and should be edited manually.

domain: package-authoring
prompts:
    - name: generate-package.txt
      purpose: package-generation
      description: System prompt for AI-assisted package manifest generation
    - name: parse-document.txt
      purpose: document-parsing
      description: System prompt for parsing installation documentation
schemas:
    - name: package-output.json
      purpose: package-output
      description: JSON schema for generated package manifest
    - name: parse-output.json
      purpose: parse-output
      description: JSON schema for parsed documentation output
examples:
    - name: generate-examples.yaml
      purpose: generation-few-shot
      description: Few-shot examples for package generation
    - name: parse-examples.yaml
      purpose: parsing-few-shot
      description: Few-shot examples for document parsing
