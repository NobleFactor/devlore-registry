# Auto-generated file list by: go run ./cmd/gen-index
# Metadata (purpose, source_system, description) is preserved and should be edited manually.

domain: shared
prompts:
    - name: clarify.txt
      purpose: clarification
      description: System prompt for asking clarifying questions
schemas:
    - name: clarification.json
      purpose: clarification
      description: JSON schema for clarification questions
examples:
    - name: edge-cases.yaml
      purpose: edge-cases
      description: Edge case examples shared across domains
slots:
    - name: definitions.yaml
      purpose: slot-definitions
      description: Standard slot definitions for template variables
providers:
    - name: providers.yaml
      purpose: provider-config
      description: AI model provider configuration and default input limits
    - name: litellm-cache.json
      purpose: model-limits
      description: Model context limits cached from LiteLLM (auto-synced)
